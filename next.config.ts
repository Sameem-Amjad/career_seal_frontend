import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["cdn-icons-png.flaticon.com", "res.cloudinary.com"], // Add Cloudinary if needed
  },
};

export default nextConfig;
